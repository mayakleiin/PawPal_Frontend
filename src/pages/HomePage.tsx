import HeroCarousel from "../components/HeroCarousel";

const HomePage = () => {
  return (
    <div className="flex flex-col items-center bg-pink-50 min-h-screen">
      <HeroCarousel />
    </div>
  );
};

export default HomePage;
